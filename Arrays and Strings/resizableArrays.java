ArrayLists
resizable factor : 2
implements list interface extends dictionary
ArrayList<Integer> arrayList = new ArrayList<Integer>();
.add(value);
.remove(index);
.size();
.get(index);
.set(index,value);
Amortized complexity = O(1) for each insertion
.clear();
.clone();
.indexOf();
