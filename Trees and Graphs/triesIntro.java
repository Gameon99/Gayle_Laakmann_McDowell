Trie - Also called prefix tree
n-ary tree 
each node - character
each path - may represent a word
* node = null node (used to represent end of the word)
tells us if a string is a prefix of any valid words in  O(k) time, where K is the length of the string.
